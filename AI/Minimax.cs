namespace Backbone.AI
{
    public static class Minimax
    {
        /*
        public static float Calculate(
            int depth,
            int maxDepth,
            bool isMax,
            Func<IGameState, float> calculationFunction,
            Func<IMoveState, IGameState> getNextGameStateFunction,
            IMoveState[] possibleMoves
        )
        {
            if (depth == maxDepth || possibleMoves.Length == 0)
            {
                return calculationFunction(currentState);
            }
            else
            {
                if (isMax)
                {
                    return gameStates.Select(gameState =>
                    {

                    })
                }
            }
        }*/
    }
}
