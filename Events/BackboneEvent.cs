namespace Backbone.Events
{
    public enum BackboneEvent
    {
        InputBoxAdd, // Add whatever is in the payload to the end
        InputBoxRemoveLast // Remove the last character from current value
    }
}
