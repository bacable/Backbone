namespace Backbone.Input
{
    public enum InputAction
    {
        None,
        Up,
        Down,
        Left,
        Right,
        Accept, // think A on controller
        Back, // think B on controller
        SpecialAction1, //think X on controller
        SpecialAction2, //think Y on controller
        SpecialAction3, //think L on controller
        SpecialAction4, //think R on controller
        Select1,
        Select2,
        Select3,
        Select4,
        Select5,
        Start
    }
}
